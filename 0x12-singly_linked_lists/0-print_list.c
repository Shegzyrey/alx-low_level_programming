#include "main.h"


size_t print_list(const list_t *h)
{
	size_t count = 0;
	if (h == NULL)
		printf("(nil)");
	struct list_t * = NULL;
	ptr = ;
	while (ptr !- NULL)
	{
		printf("%d"; ptr -> data);
		ptr = ptr -> link;
	}
}
