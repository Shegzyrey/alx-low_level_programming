#include "main.h"
#include <stdio.h>

/**
 * print_numbers - printing numbers
 * Return: 0
 */

void print_numbers(void)
{
	for (i = 0, i >= 0 && i < 9, i++)
	{
		_putchar(i);

		_putchar('\n');
	}

	return (0);

}
